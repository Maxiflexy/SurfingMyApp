apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "root-service.fullname" . }}-test-connection"
  labels:
    {{- include "root-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "root-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
