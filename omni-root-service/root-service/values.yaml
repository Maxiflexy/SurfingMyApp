# Default values for root-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: 549258854334.dkr.ecr.eu-west-2.amazonaws.com/gateway-root-service
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80
  targetPort: 2759

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: 
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 10000m
    memory: 1024Mi
  requests:
    cpu: 2000m
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  #targetMemoryUtilizationPercentage: 70


efs:
  filesystemId: fs-0eadfde87b023f93e  # Replace with your EFS filesystem ID
  accessPointId: fsap-02be49a3200c4ff82  # Replace with your EFS access point ID
  mountPaths:
    - path: /mnt/paymentgateway/omni/Logo
    - path: /mnt/paymentgateway/omni/Proof Of Address
    - path: /mnt/paymentgateway/omni/International Passport
    - path: /mnt/paymentgateway/omni/Driving License
    - path: /mnt/paymentgateway/omni/Voter's Card
    - path: /mnt/paymentgateway/omni/National ID
    - path: /mnt/paymentgateway/omni/CAC Registration Certificate
    - path: /mnt/paymentgateway/omni/NIN
    - path: /mnt/paymentgateway/omni/SCUML
    - path: /mnt/paymentgateway/omni/MEMART
    - path: /mnt/paymentgateway/omni/Director Particulars
    - path: /mnt/paymentgateway/omni/Incorporation Certificate
      

nodeSelector: {}

tolerations: []

affinity: {}
